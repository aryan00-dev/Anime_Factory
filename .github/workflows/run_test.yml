name: Veda Cloud Engine Auto-Post

on:
  schedule:
    - cron: '30 12 * * *' 
  workflow_dispatch: # YEH LINE HONA ZAROORI HAI BUTTON KE LIYE

jobs:
  run-bot:
    runs-on: ubuntu-latest
# ... baaki ka code ...
